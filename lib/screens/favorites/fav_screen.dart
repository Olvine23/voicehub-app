import 'package:flutter/material.dart';

class FavScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Fav Screen'),
      ),
      body: Center(
        child: Text('Fav Screen Content'),
      ),
    );
  }
}